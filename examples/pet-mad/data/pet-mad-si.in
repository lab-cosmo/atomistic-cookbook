units metal  # Angstroms, eV, picoseconds 
atom_style atomic
read_data silicon.data

# loads pet-mad-model
pair_style metatensor &
    pet-mad-latest.pt &
    device cpu &
    extensions extensions/

# define interactions between all atoms and maps the LAMMPS types to elements
pair_coeff * * 14

neighbor 2.0 bin
timestep 0.0005

dump myDump all xyz 20 trajectory.xyz
dump_modify myDump element Si

thermo_style multi
thermo 1

fix 1 all nve
fix 2 all langevin 310 310 1.00 12345 

run 200
